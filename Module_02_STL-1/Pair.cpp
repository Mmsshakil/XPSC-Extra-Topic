#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // pair<string, int> student = make_pair("Karim", 75);
    // pair<string, int> student = {"Karim", 75};

    // auto [name, roll] = student;
    // cout << name << " " << roll << endl;
    // cout << student.first << " " << student.second << endl;

    // pair in array
    // int n;
    // cin >> n;
    // pair<string, int> students[n];

    // for (int i = 0; i < n; i++)
    // {
    //     cin >> students[i].first >> students[i].second;
    // }

    // for (int i = 0; i < n; i++)
    // {
    //     cout << students[i].first << " " << students[i].second << endl;
    // }

    // this part will not work in my vscode
    // for (auto  [x, y] : students)
    // {
    //     cout << x << " " << y << endl;
    // }

    // pair of a pair
    // pair<string, pair<int, int>> p = {"shakil", {75, 123456}};

    // cout << p.second.first << endl;

    return 0;
}